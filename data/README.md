# Data Directory

Place the public OpenHSR file here as:

```text
data/OpenHSR.csv
```

You can download it with:

```bash
python scripts/download_openhsr.py --output data/OpenHSR.csv
```

Private product records are not stored in this repository.
